# Linux deploy safe defaults
* text=auto

# Scripts must be LF
gradlew text eol=lf
*.sh text eol=lf

# Windows scripts should be CRLF (선택: 팀이 윈도우에서 직접 실행하면 유지 권장)
*.bat text eol=crlf
*.cmd text eol=crlf

# Configs are safer as LF
*.yml text eol=lf
*.yaml text eol=lf
*.properties text eol=lf
*.env text eol=lf
*.sql text eol=lf